# tiktok-watch-history-automation
This repository automates the process of extracting TikTok video URLs from watch history using ADB and scrcpy, storing them in a local file, and handling video downloads separately. The system includes error handling for internet issues and TikTok updates, ensuring smooth navigation, retry options, and logging.
